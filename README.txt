## Project Setup
- Virtual Environment (Python 3.11.2)
- python -m venv .venv (Windows) // python3 -m venv .venv (MacOS)
- venv\Scripts\activate (Windows) // source .venv/bin/activate (MacOS)
- python main.py (Windows)  // python3 main.py (MacOS)
